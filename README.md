# C File Analyzer

A simple command-line utility written in C that analyzes a text file and reports
basic statistics such as line count, word count, and character count.

## Features
- File I/O with error handling
- Modular design using header files
- Command-line argument parsing
- Clear separation between interface and implementation

## How to Build
```bash
make